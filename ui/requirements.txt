# DROCAT UI Requirements
# Install with: pip install -r ui/requirements.txt
# Exact versions tested with the v4.5.0 UI. Pydantic is explicit so pip cannot
# pair a newly released pydantic-core with an older Pydantic installation.
nicegui==3.15.0
pydantic==2.13.4
Jinja2==3.1.6
