# VisualizePath Sub-project - Requirements

# Core Dependencies
numpy>=1.20.0,<2.0.0       # Keep <2.0.0 for binary compatibility with pandas 1.x
pandas>=1.3.0,<2.0.0
scipy>=1.7.0
plotly>=5.0.0
polars>=1.0.0               # Memory-efficient DataFrames for large data operations
openpyxl>=3.0.0

# Note: NetworkX is no longer required - replaced by built-in FastGraph module

# Optional (GUI for file dialogs)
PyQt5>=5.15.0
