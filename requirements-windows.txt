# =============================================================================
# Hemibrain Connectomes Analysis - Windows Requirements
# Project Structure: Reorganized with src/ layout (v4.1)
# Versions pinned to tested working versions from conda env 'drocat'
# =============================================================================
# Windows uses the same tested stack as macOS/Linux. NeuronBridge access is
# provided by the bundled client and no longer pulls Linux-only Memray.
# =============================================================================

# Core Dependencies
# -----------------------------------------------------------------------------
numpy==1.26.4              # NumPy - tested version (do not upgrade to 2.x)
pandas==1.5.3              # Pandas - tested 1.x version
scipy==1.13.1              # Scientific computing (clustering, etc.)
pyarrow==17.0.0            # Parquet file support for fast caching

# Visualization
# -----------------------------------------------------------------------------
plotly==6.4.0              # Interactive plots, Sankey diagrams
matplotlib==3.10.0         # Static plots
seaborn==0.13.2            # Statistical visualizations
opencv-python==4.10.0.84   # Computer vision (video generation, image processing)
bokeh==3.6.0               # Additional visualization (palettes)
kaleido==0.2.1             # Plotly static image export (0.2.1 is stable for large 3D figures)
selenium>=4.0.0,<5         # Browser automation for WebGL-based exports
webdriver-manager>=4.0.0,<5 # Automatic ChromeDriver management

# Network Analysis
# -----------------------------------------------------------------------------
networkx==3.3              # Graph algorithms and network analysis

# Data Processing
# -----------------------------------------------------------------------------
openpyxl==3.1.5            # Excel file support (.xlsx)
xlrd==2.0.2                # Legacy Excel file support (.xls)
tqdm==4.66.5               # Progress bars
requests==2.32.3           # HTTP requests
chardet==5.2.0             # Keep requests diagnostics compatible
Jinja2==3.1.6              # HTML templating (required by NiceGUI 3.15)
pillow==11.0.0             # Image processing
polars==1.34.0             # Memory-efficient DataFrames for large cache operations
pydantic==2.13.4           # Exact match for the NiceGUI/FastAPI runtime

# GUI Backends (for file picker and sheet selection dialogs)
# -----------------------------------------------------------------------------
PyQt5==5.15.10             # Recommended - Fastest, most responsive dialogs

# Neuroscience Data
# -----------------------------------------------------------------------------
neuprint-python==0.4.26    # NeuPrint database access
navis==1.5.0               # Neuron visualization and analysis toolkit
flybrains==0.6.3           # Adds VNC mesh support; constrains h5py<=3.12.1
zstandard==0.23.0          # Compressed FlyWire MeshNeuron cache (.pkl.zst)

# CAVE API / FlyWire Integration (v4.3.4)
# -----------------------------------------------------------------------------
caveclient>=8.0.0,<9       # CAVE materialization API for FlyWire FAFB
cloud-volume>=12.0.0,<13   # CloudVolume for mesh fetching via graphene protocol
fast-simplification>=0.1.0,<0.2 # Mesh simplification used by trimesh 4.x
trimesh==4.12.2            # Last stable 4.x API; avoid the incompatible 5.x line

# NeuronBridge & FlyLight Integration (v4.3.0)
# -----------------------------------------------------------------------------
boto3>=1.26.0,<2           # AWS S3 access for FlyLight imagery downloads
reportlab>=4.0.0,<6        # PDF generation for image summaries
python-pptx>=0.6.21,<2     # PowerPoint generation for image summaries
pymupdf>=1.23.0,<2         # PDF-to-PPTX conversion (import name: fitz)

# Document Processing
# -----------------------------------------------------------------------------
img2pdf==0.5.1             # Convert images to PDF

# Interactive 3D Visualization
# -----------------------------------------------------------------------------
k3d==2.17.0                # High-performance 3D visualization (WebGL)
ipywidgets==8.1.5          # Required for k3d HTML export
open3d==0.19.0             # 3D mesh processing - REQUIRED for accurate mesh simplification
                           # Note: trimesh 4.x uses fast-simplification which only achieves ~60%
                           # reduction on neuron tube meshes. open3d achieves exact target (90%+).

# System Utilities
# -----------------------------------------------------------------------------
psutil>=5.9.0,<8           # System monitoring utilities (memory usage tracking)
xlsxwriter>=3.1.0,<4       # Excel file generation (.xlsx)
packaging>=21.0            # Requirement parsing for the install verifier

# NeuronBridge API (EM↔LM Integration)
# -----------------------------------------------------------------------------
# Implemented by src/neuronbridge_client.py with requests + Pillow.

# Optional Dependencies (uncomment as needed)
# -----------------------------------------------------------------------------
# cloud-volume             # CloudVolume for mesh/skeleton data (large dependency)
# PyQt6                    # Alternative to PyQt5
# wxPython                 # Alternative GUI backend

# Development and Testing (Optional)
# -----------------------------------------------------------------------------
# pytest==7.0.0            # Testing framework
# pytest-cov==3.0.0        # Coverage reporting
# black==22.0.0            # Code formatter
# flake8==4.0.0            # Linter

# Jupyter Notebook Support (Optional)
# -----------------------------------------------------------------------------
# jupyter==1.0.0
# ipykernel==6.0.0
# notebook==6.4.0

# =============================================================================
# Windows Installation Instructions
# =============================================================================
# Install all dependencies from this file:
#   pip install -r requirements-windows.txt
# Development installation (modifying the source code):
#   pip install -e .
#   pip install -r requirements-windows.txt
#
# The -e flag means "editable" - changes to source files take effect immediately
# without needing to reinstall. Use this if you're developing/modifying the code.
