# =============================================================================
# Hemibrain Connectomes Analysis - Requirements
# Project Structure: Reorganized with src/ layout (v3.0.0)
# =============================================================================

# Core Dependencies
# -----------------------------------------------------------------------------
numpy>=1.20.0,<2.0.0       # Keep <2.0.0 for binary compatibility with pandas 1.x
pandas>=1.3.0,<2.0.0      # Note: Keep <2.0.0 for compatibility
scipy>=1.7.0               # Used for clustering (scipy.cluster.hierarchy)

# Visualization
# -----------------------------------------------------------------------------
plotly>=5.0.0              # Interactive plots, Sankey diagrams (plotly.graph_objects, plotly.io)
matplotlib>=3.4.0          # Static plots (matplotlib.pyplot, matplotlib.patches)
seaborn>=0.11.0            # Statistical visualizations
opencv-python>=4.5.0       # Computer vision library (cv2 - video generation, image processing)
bokeh>=2.4.0               # Additional visualization library (bokeh.palettes)

# Network Analysis
# -----------------------------------------------------------------------------
networkx>=2.6.0            # Graph algorithms and network analysis

# Advanced Graph Layout (Optional - for better crossing minimization)
# -----------------------------------------------------------------------------
# Uncomment for advanced Python-based layouts (not needed for web visualizations)
# pygraphviz>=1.11         # Graphviz integration - best for hierarchical layouts
# grandalf>=0.8            # Modern Sugiyama layout implementation
# networkit>=10.0          # High-performance layouts for large graphs (10k+ nodes)
# graph-tool               # Research-grade layouts (install via conda: conda install -c conda-forge graph-tool)

# Data Processing
# -----------------------------------------------------------------------------
openpyxl>=3.0.0            # Excel file support (.xlsx)

# GUI Backends (for file picker and sheet selection dialogs)
# -----------------------------------------------------------------------------
# Install at least one for optimal performance (10-50x faster than tkinter)
PyQt5>=5.15.0              # Recommended - Fastest, most responsive dialogs
# PyQt6>=6.0.0             # Alternative to PyQt5 (uncomment if preferred)
# wxPython>=4.2.0          # Alternative GUI backend (uncomment if preferred)

# Neuroscience Data
# -----------------------------------------------------------------------------
neuprint-python>=0.4.0     # NeuPrint database access for hemibrain data
navis>=1.0.0               # Neuron visualization and analysis toolkit (required by coana.py, statvis.py)
flybrains>=0.2.0           # Drosophila brain templates and meshes (required by coana.py)

# Document Processing
# -----------------------------------------------------------------------------
img2pdf>=0.4.0             # Convert images to PDF (required by statvis.py)

# Interactive 3D Visualization (Optional - for skeleton plots)
# -----------------------------------------------------------------------------
# Uncomment if you need additional 3D neuron skeleton visualization
# Note: navis is already included above as a core requirement
# trimesh>=3.9.0           # 3D mesh processing
# vispy>=0.11.0            # GPU-accelerated visualization
# pygfx>=0.1.0             # Modern 3D rendering
k3d>=2.15.0                # High-performance 3D visualization (WebGL)
ipywidgets>=8.0.0          # Required for k3d HTML export
open3d>=0.15.0             # 3D mesh processing and simplification

# Development and Testing (Optional)
# -----------------------------------------------------------------------------
# Uncomment for development
# pytest>=7.0.0            # Testing framework
# pytest-cov>=3.0.0        # Coverage reporting
# black>=22.0.0            # Code formatter
# flake8>=4.0.0            # Linter
# mypy>=0.950              # Type checker

# Jupyter Notebook Support (Optional)
# -----------------------------------------------------------------------------
# Uncomment if you use notebooks
# jupyter>=1.0.0
# ipykernel>=6.0.0
# ipywidgets>=8.0.0
# notebook>=6.4.0

# System Notes
# -----------------------------------------------------------------------------
# - tkinter is included with Python by default (no pip install needed)
# - On Linux, if tkinter is missing: sudo apt-get install python3-tk
# - For optimal performance, PyQt5 is recommended for GUI dialogs
# - All scripts in scripts/ directory import from src/ automatically

# Installation
# -----------------------------------------------------------------------------
# Standard installation:
#   pip install -r requirements.txt
#
# Development installation:
#   pip install -e .
#
# With optional dependencies:
#   pip install -e ".[dev,viz,all]"
